---
// src/pages/shop.astro snippets
import Layout from '../layouts/Layout.astro';
import merchandise from '../data/shop.json';
---

<script>
  // Dynamically inherit the image array map inside client scripts:
  import merchandise from '../data/shop.json';
  const shopImages = merchandise.map(item => item.image);
  
  // Lightbox functions use shopImages dynamically...
</script>